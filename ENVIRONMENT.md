# Environment provenance

Submission rendering only: Python 3.13.9, matplotlib 3.10.6; DejaVu Sans; vector PDF/SVG with rasterized dense scatter points and 200-dpi PNG previews. This environment is not attributed to historical analyses.

Recorded raw processing: Cutadapt 4.1; Bowtie2 2.2.6; TopHat 2.1.0; samtools 1.6. Validation 1: Python 3.12.14, HTSeq 2.0.3, pysam 0.24.1, NumPy 1.26.4, samtools 1.23.1. Validation 3: Python 3.12.14, NumPy 1.26.4, pandas 2.2.3, statsmodels 0.14.6, openpyxl 3.1.5. Exact original evolutionary/association environments and complete seed/default inventory remain unresolved. No fabricated lockfile or claim of cross-platform bitwise identity is supplied.


## Execution status and reproducibility limits
No historical analysis was executed during final packaging. Recorded versions are documentary evidence, not an assertion that all dependencies coexisted in one environment. Freeze files and software_versions.json are authoritative where present; a new combined requirements lockfile would not reconstruct missing historical versions and has not been fabricated. Original scripts may need the recorded macOS/Intel-compatible runtimes and the external input tree. Runtime installation, path adaptation and end-to-end execution are not validated in this candidate.

## Submission PDF assembly (separate environment)
The final packaging used the installed Codex bundled Python with ReportLab and pypdf, and Poppler for visual rendering. Packaging runtime versions are recorded separately in PACKAGING_ENVIRONMENT.json. They must not be attributed to the historical analyses. PDF assembly does not fit or recalculate scientific results.
